{
  "name": "ai2fi",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "detect-splits": "node scripts/split-detector.js",
    "server": "node server/index.js",
    "client": "npm --prefix client run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install:all": "npm install && npm --prefix client install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "exceljs": "^4.4.0",
    "express": "^4.19.2",
    "xlsx": "^0.18.5",
    "yahoo-finance2": "^3.14.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
